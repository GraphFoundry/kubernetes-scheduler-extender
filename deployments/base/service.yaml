apiVersion: v1
kind: Service
metadata:
  name: scheduler-extender
  namespace: default
spec:
  selector:
    app: scheduler-extender
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
