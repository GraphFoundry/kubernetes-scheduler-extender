# Custom Scheduler Configuration
# Copy this file to .env and adjust values as needed

# Best Node Selector API URL
# Use host.minikube.internal when running scheduler inside minikube
BEST_NODE_SELECTOR_URL=http://host.minikube.internal:9000

# Kubernetes config (optional, uses in-cluster config if not set)
# KUBECONFIG=~/.kube/config
