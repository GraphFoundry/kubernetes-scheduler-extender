apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-feed
  labels:
    app: backend-feed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-feed
  template:
    metadata:
      labels:
        app: backend-feed
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      schedulerName: my-scheduler
      containers:
        - name: backend-feed
          image: nginx:alpine
          ports:
            - containerPort: 80
          env:
            - name: DATABASE_URL
              value: "postgres://postgres:example@database:5432/postgres"
        - name: traffic-simulator
          image: alpine
          command: ["/bin/sh", "-c"]
          args:
            [
              "apk add --no-cache netcat-openbsd && while true; do nc -z database 5432; sleep 0.1; done",
            ]

---
apiVersion: v1
kind: Service
metadata:
  name: backend-feed
  labels:
    app: backend-feed
spec:
  ports:
    - port: 80
      name: http
  selector:
    app: backend-feed
